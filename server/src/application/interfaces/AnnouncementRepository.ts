// import { Announcement } from "../../domain/entities/Announcement";

// export interface AnnouncementRepository {
//   createAnnouncement(announcement: Partial<Announcement>): Promise<Announcement>;
//   getAnnouncementById(id: string): Promise<Announcement | null>;
//   updateAnnouncement(id: string, announcementData: Partial<Announcement>): Promise<Announcement>;
//   deleteAnnouncement(id: string): Promise<void>;
//   getAllAnnouncements(): Promise<Announcement[]>;
//   getAnnouncementsByStatus(status: 'active' | 'inactive'): Promise<Announcement[]>;
// }

